# 抖音海龟汤 V7 实施纲要

> 基于 V6 代码（server_v6.py / overlay.py / admin.py / dashboard.py / gift_slots.py / tiers.py / data_soups_v6.py / dy_bridge.py / config_loader.py）逆向提炼。
> V6 设计文档（CLAUDE实施手册_v6.0.md）仅供需求考古参考，不再作为实施依据。

---

## 1. 架构总览

```
┌─────────────────────────────────────────────────────┐
│                  single process                      │
│              FastAPI + WebSocket + SQLite            │
│                    port 3010                         │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │  admin    │  │dashboard │  │ overlay          │   │
│  │ (控制台)  │  │ (数据)   │  │ (投屏)           │   │
│  │ 5个Tab    │  │ 4指标卡  │  │ 9:16竖屏         │   │
│  │ 主播操作  │  │ 趋势图   │  │ 只显示不操作     │   │
│  └────┬─────┘  └────┬─────┘  └────────┬─────────┘   │
│       └──────────────┼─────────────────┘             │
│                      ▼                               │
│               WebSocket /api/*                        │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │dy_bridge │  │gift_slots│  │   llm_client     │   │
│  │WS→HTTP推 │  │10槽位管理│  │classify/hint/gen │   │
│  └──────────┘  └──────────┘  └──────────────────┘   │
└─────────────────────────────────────────────────────┘
```

### 1.1 三端分离

| 端 | URL | 运行位置 | 用途 |
|----|-----|---------|------|
| admin (控制台) | `/admin` | 主播电脑 Chrome | 操作：开局/换题/调难度/管礼物槽 |
| dashboard (数据) | `/dashboard` | 主播电脑 Chrome/副屏 | 看：在线/弹幕/礼物/段位分布 |
| overlay (投屏) | `/overlay` | OBS 浏览器源 | 直播画面，观众看到的内容 |

### 1.2 外部依赖

| 组件 | 说明 |
|------|------|
| douyinLive.exe (port 1088) | Go 二进制，抖音直播间 WS 抓包 → 推送给 dy_bridge |
| gift_icons.json | 368+ 抖音礼物信息（名称/抖币/图标URL），加载到 GIFT_LIBRARY |
| LLM API (DeepSeek) | 三分类 + 方向提示 + AI 出题 |
| .env | 本地配置文件 |

---

## 2. 游戏流程（状态机）

```
lobby → reading → playing → complete → (5s) → reading
```

### 2.1 状态说明

| 状态 | 触发 | 行为 |
|------|------|------|
| `lobby` | 服务启动 / 管理员重置 | 等待开局 |
| `reading` | 管理员点击"开始" | TTS 朗读汤面 → 自动进入 playing |
| `playing` | 朗读结束 | 弹幕猜谜（Track A + Track B 同时进行） |
| `complete` | 100%揭示 / 管理员"揭晓" | TTS 朗读汤底 → 5s 后自动下一局 |
| `注意` | — | reading→playing 自动转换未实现。`start_game()` 设 `phase="reading"` 后无定时器推进状态。`handle_danmaku` 在 reading 和 playing 两阶段均处理弹幕（功能可运行但状态机不完整） |

### 2.2 开局流程（start_game）

1. 按 `difficulty` 从 SOUPS 中筛选候选题；若该难度无候选（如 hell/void 无内置题），回退到全部 SOUPS 随机选
2. 如果 room.next_difficulty 有值（礼物切换），覆盖当前难度并清空 next_difficulty
3. 调用 GameRoom.reset() 清空所有状态
4. 用 RevealEngine.init_char_states() 初始化字符状态（实词隐藏、虚词显示）
5. 设置 phase = "reading"，记录 start_time 和 anti_last_reveal
6. 广播 `game_start` 消息到所有 WebSocket 客户端

### 2.3 弹幕处理双轨道（handle_danmaku）

每一条弹幕同时走两条轨道。守卫条件：text 非空、soup_answer 非空、phase 为 playing 或 reading。

**Track A — 逐字揭示（本地快速匹配）**

```
弹幕中的每个字 → 是否在汤底中 + 是否实词 → 是则揭示 → 更新 charStates
```

- 虚词（的了是在和吗呢吧…）不匹配、不揭示
- 已揭示的字不重复揭示
- 匹配到新字 → 重置 anti_stall 计时器（anti_last_reveal、anti_since_reveal、anti_triggers 三个字段均归零）
- 广播 `reveal_update`（含 charStates 和 newChars 列表）

**Track B — 问答分类（LLM）**

```
弹幕 → llm_classify(弹幕, 汤底, 关键词) → 三分类结果
```

- `是`：玩家加分 = base × difficulty_multiplier
- `是也不是`：玩家加分 = base × difficulty_multiplier / 2
- `不是`：不加分
- 结果写入 qa_history，广播 `classification` 消息

**通关判定**

每次 reveal_update 后计算揭示百分比：
- 100% 实词已揭示 → end_game(reveal=True)
- 广播 `game_end`（含完整汤底）

### 2.4 结束流程（end_game）

1. 所有字符设为 revealed
2. phase = "complete"
3. 广播 `game_end`（含 charStates、winner（硬编码 "系统"）、汤底）
4. overlay 端显示结算弹窗 + TTS 朗读汤底

> **注意**：`winner` 始终为 "系统"，不追踪实际第一个通关玩家。如有需求应改为记录达成 100% 揭示前的最后答对用户。

### 2.5 防卡死机制（anti_stall_loop，30s 轮询）

```
条件：(当前时间 - 上次揭示 >= 180s × decay^triggers)
    OR (无揭示弹幕数 >= 50 × decay^triggers)
触发：揭示最高频出现的未揭示实词
广播 reveal_update + 防卡死提示（60s 后自动消失）
```

- `ANTI_STALL_INTERVAL`（默认180s）基础间隔
- `ANTI_STALL_DECAY`（默认0.8）每次触发后间隔缩短
- `ANTI_STALL_DANMAKU`（默认50条）弹幕阈值
- 提示自动消失时间：60s（CONFIG.hintDisappearMs）
- 守卫条件：仅 `phase in ("playing","reading")` 时生效，lobby/complete 跳过；无汤底时跳过；全部揭示后跳过
- 自身异常保护：循环体被 try/except 包裹，异常时打印日志后继续下一次 30s 轮询，不会中断循环

> **autoHide 单位 Bug**：服务端 anti_stall_loop 发送 `autoHide: 60`（秒单位），但 overlay.js `showHint(text, autoHideMs)` 用 `setTimeout(..., autoHideMs)` 解释为毫秒。结果防卡死提示实际仅显示 60ms 后消失。应统一：服务端发 `autoHide: 60000` 或客户端乘以 1000。

> **防卡死揭示视觉差异**：overlay.js flashChar() 对 `isAntiStall=true` 的揭示字加 `box-shadow: 0 0 30px var(--red)` 红色边框闪烁，与普通揭示的金色高亮区分。V7 实施时保留此设计。

---

## 3. 字符揭示引擎（RevealEngine）

### 3.1 实词/虚词判定

```python
FUNCTION_WORDS = set("的了是在和吗呢吧着过得地个一不没有 就都而但又如果因为所以然后于是向对从到把被给让每只想会能可以很太非常已经正在曾经将要这那你我他她它们上下里外前后中时还也再才刚做说看来去")

def is_content_word(ch: str) -> bool:
    return bool(re.match(r"[一-鿿]", ch)) and ch not in FUNCTION_WORDS
```

- 匹配汉字（Unicode 范围 `一-鿿`）且不在虚词表 → 实词（默认隐藏）
- 非汉字（标点/数字/字母）或虚词 → 直接显示（function-word）

### 3.2 字格状态

```python
char_state = {
    "char": "字",        # 字符本身
    "revealed": False,   # 是否已揭示
    "isContent": True,   # 是否实词
}
```

### 3.3 揭示方法

| 方法 | 行为 |
|------|------|
| `reveal_char(states, target)` | 匹配所有相同字 |
| `reveal_sentence(states, answer)` | 按句号分割，揭示第一个有未揭示实词的整句 |
| `reveal_pct(states, pct)` | 按百分比揭示未揭示字（用于 gift 30% 效果）。**注意**：`int(len(unrevealed) * pct)` 做整数截断，当未揭示实词 ≤3 个时 30% 效果揭示 0 字。 |

> **注意**：`reveal_sentence()` 对 `states` 做原地修改（mutable），无需返回值。`reveal_char()` 和 `reveal_pct()` 也直接修改传入列表。所有揭示方法均就地操作。
| `get_progress(states)` | 返回 (revealed, total, pct) |

---

## 4. WebSocket 协议

### 4.1 连接

```
ws://host:3010/ws
```

连接建立后立即推送 `state_sync` 消息（to_dict() 返回的 13 字段 GameRoom 投影）。

> **注意**：V6 中 `websocket_endpoint` 的 `json.loads(raw)` 无 try/except 保护。客户端发送非 JSON 消息时抛出 `JSONDecodeError`，连接断开但 `manager.disconnect()` 未被调用 → 连接泄漏。V7 应在 json.loads 外层加 try/except，忽略无效消息。

### 4.2 客户端可以发送

| type | 字段 | 说明 |
|------|------|------|
| `danmaku` | `text`, `nickname` | 弹幕内容 |
| `gift` | `giftName`, `nickname`, `diamond_count` | 礼物触发 |
| `start_round` | `difficulty`, `soup_id`(可选) | 管理员开局 |
| `end_round` | - | 管理员结束 |
| `ping` | - | 心跳 → 回复 `pong` |

> **注意**：WS 客户端使用 camelCase 命名（`giftName`/`nickname`/`diamond_count`），而 HTTP 推流端点使用 snake_case（`gift_name`/`user`/`diamond_count`）。服务端 `handle_danmaku`/`handle_gift` 内部兼容两种命名格式。

### 4.3 服务端广播

| type | 关键字段 | 说明 |
|------|----------|------|
| `state_sync` | `room`（to_dict() 返回的 13 字段投影，含 phase/surface/charStates/stats 等，不含 soup_answer/qa_history 等原始字段） | 连接建立/重置后推送 |
| `game_start` | `surface`, `difficulty`, `difficulty_name`, `charStates`, `soup_id` | 新局开始 |
| `reveal_update` | `charStates`, `newChars[]`, `antiStall`, `highlight` | 揭示更新 |
| `classification` | `text`, `user`, `answerType`(是/不是/是也不是) | Track B 结果 |
| `hint` | `hint`, `script`, `user`, `autoHide`(秒，仅 anti_stall 携带，礼物触发的 hint 不带此字段) | 方向提示/防卡死提示 |
| `gift_effect` | `user`, `giftName`, `slotId`, `coins` | 礼物触发效果 |
| `game_end` | `charStates`, `winner`, `bottom`, `soup_id` | 游戏结束 |
| `tier_up` | `user`, `from_tier`, `to_tier`, `delta`, `score` | 段位升级 |
| `score_update` | `user`, `score`, `tier` | 积分变化 |
| `difficulty_scheduled` | `nextDifficulty`, `nextDifficultyName` | 难度切换预告 |
| `slots_updated` | `slots[]` | 礼物槽位变更 |
| `metrics_update` | `metrics` | 数据统计推送（预留） |

---

## 5. REST API

### 5.1 游戏控制

| 方法 | 路径 | 请求体 | 说明 |
|------|------|--------|------|
| GET | `/api/game/state` | - | 返回当前 GameRoom 全部状态 |
| POST | `/api/game/start` | `{difficulty, soup_id?}` | 开始新局 |
| POST | `/api/game/end` | - | (通过 WS end_round 实现) |
| GET | `/api/game/difficulties` | - | 返回 5 档难度定义 |

### 5.2 管理员

| 方法 | 路径 | 请求体 | 说明 |
|------|------|--------|------|
| POST | `/api/admin/difficulty` | `{difficulty}` | 设置下局难度 |
| POST | `/api/admin/force-reveal` | - | 强制揭晓全部答案 |
| POST | `/api/admin/reset` | - | 重置游戏到 lobby（广播 state_sync） |
| GET | `/api/admin/slots` | - | 10 槽位状态 + 绑定礼物 |
| POST | `/api/admin/slots/assign` | `{slot_id, gift_name}` | 绑定礼物到槽位（广播 slots_updated） |
| GET | `/api/admin/gifts/search` | `?q=` | 搜索礼物库 |
| GET | `/api/admin/soups` | `?difficulty=` | 题库列表 |
| POST | `/api/admin/soups/add` | `{title,surface,bottom,keywords,difficulty}` | 新增题目 |
| POST | `/api/admin/soups/delete` | `{id}` | 删除题目 |
| POST | `/api/admin/soups/import` | `{soups[]}` | 批量导入 |
| POST | `/api/admin/ai-generate` | `{difficulty, count}` | AI 生成题目 |
| POST | `/api/admin/ai-approve` | `{soup}` | AI 题审核入库 |
| GET | `/api/admin/metrics` | - | 实时数据指标 |
| GET | `/api/admin/tier-dist` | - | 段位分布 |
| GET | `/api/admin/export` | `?format=json` | 导出数据 |
| GET | `/api/leaderboard` | `?limit=10` | 排行榜 |
| GET | `/health` | - | 健康检查（状态/版本/连接数） |

### 5.3 弹幕/礼物推流（dy_bridge → server）

| 方法 | 路径 | 请求体 | 说明 |
|------|------|--------|------|
| POST | `/api/barrage/push` | `{user, content}` | 弹幕推流 |
| POST | `/api/gift/push` | `{user, gift_name, diamond_count, count}` | 礼物推流 |

---

## 6. 礼物系统

### 6.1 10 个固定槽位

**5 个效果槽（立即生效）：**

| 槽位 ID | 名称 | 效果 | 默认绑定 |
|---------|------|------|---------|
| `effect_highlight` | 高亮线索 | 揭示最高频未揭示实词 + 金色高亮动画 | 小心心 |
| `effect_hint` | 方向提示 | LLM 生成解谜提示（≤20字），显示为中下提示横幅 | 人气票 |
| `effect_reveal1` | 揭示一字 | 随机揭示一个未揭示实词 | 啤酒 |
| `effect_reveal_sentence` | 揭示一句 | 按标点分割，揭示第一个含未揭示字的整句 | 棒棒糖 |
| `effect_reveal_30p` | 揭示30% | 立即揭示 30% 的未揭示实词 | 墨镜 |

**5 个难度槽（下局生效）：**

| 槽位 ID | 名称 | 效果 | 默认绑定 |
|---------|------|------|---------|
| `diff_easy` | 难度-简单 | 下局 30-50字 | 鲜花 |
| `diff_medium` | 难度-一般 | 下局 50-80字 | 玫瑰 |
| `diff_hard` | 难度-困难 | 下局 80-100字 | 跑车 |
| `diff_hell` | 难度-地狱 | 下局 100-120字 | 嘉年华 |
| `diff_void` | 难度-无人区 | 下局 120-150字 | 梦幻城堡 |

### 6.2 礼物解析流程

```
收到礼物名 → slot_manager.resolve_gift() 遍历10个槽位
→ 匹配 enabled == True 且 gift_name 一致的 → 返回 slot_id
→ 若无匹配则静默返回（不记录、不报错、无日志）
→ 按 slot_id 执行对应效果
→ 记录到 gift_log（SQLite）
→ 更新 stats.gift_list（TOP 10）、stats.gift_revenue
→ 广播 gift_effect
```

> **注意**：`handle_gift()` 接收礼物名时字段名有二义性（兼容 `giftName` 或 `gift_name`），抖币金额优先 `diamond_count` 再 `coins` 再 GIFT_LIBRARY 默认值，形成三条回退链。dy_bridge 推流用 `gift_name`/`diamond_count`，WS 客户端用 `giftName`/`coins`。

> **注意**：`handle_gift()` 忽略消息中的 `count` 字段。dy_bridge 和 PushGiftReq 均携带 `count`（连击数），但槽位效果无论 `count` 多大只触发一次。多连击礼物不会触发多次效果。如需支持，应在 `handle_gift` 中按 `count` 循环执行槽位逻辑。

> **⚠ 礼物通关判定**：`handle_gift()` 执行揭示类效果（高亮/揭示一字/揭示一句/揭示30%）后，会立即调用 `get_progress()` 检查是否 100% 揭示 → 触发 `end_game(reveal=True)`。此逻辑与弹幕 Track A 的通关判定平行。

> **⚠ `gift_series` 记录的是抖币收入**：`_record_series("gift", value=coins)` 按分钟聚合的是礼物抖币总收入而非礼物数量。dashboard 趋势图标为"礼物收入趋势"而非"礼物数趋势"。

### 6.3 礼物库加载

- 主路径：`<project>/gift_icons.json`
- 备选路径：`C:/Users/27871/OneDrive/Desktop/CCcat猜词大挑战/overlay/gift_icons.json`
- 加载到 `GIFT_LIBRARY: dict[str, {coins, icon}]`（368+ 礼物）
- 搜索 API 返回前 50 条匹配结果

---

## 7. 难度与积分系统

### 7.1 5 档难度

| ID | 名称 | 字数区间 | 倍率 | 基础分 |
|----|------|---------|------|-------|
| easy | 简单 | 30-50 | ×1.0 | 10 |
| medium | 一般 | 50-80 | ×1.5 | 15 |
| hard | 困难 | 80-100 | ×2.0 | 20 |
| hell | 地狱 | 100-120 | ×3.0 | 30 |
| void | 无人区 | 120-150 | ×5.0 | 50 |

### 7.2 积分规则

```
命中加分 = SCORE_BY_DIFFICULTY[difficulty]（基础分，easy=10, medium=15, hard=20, hell=30, void=50）
         × DIFFICULTY_CONFIG[difficulty].multiplier（×1.0/1.5/2.0/3.0/5.0）
- "是"答：full（base × multiplier），例：medium=15×1.5=22（int），void=50×5.0=250
- "是也不是"答：half（base × multiplier / 2），例：medium=11，void=125
- "不是"答：0
新用户默认分：max(100, delta)，即首次答对至少 100 分（反直觉，见 §18 已知问题）
```

### 7.3 10 级段位

| 段位 | 最低分 | 图标 |
|------|-------|------|
| 黑铁 | 0 | 🛡️ |
| 青铜 | 100 | 🥉 |
| 黄金 | 500 | 🥇 |
| 铂金 | 2000 | 💎 |
| 钻石 | 5000 | 💠 |
| 白银 | 10,000 | ⚪ |
| 王者 | 25,000 | 👑 |
| 宗师 | 50,000 | 🔥 |
| 大师 | 100,000 | 🌟 |
| 超级王者 | 200,000 | ⚡ |

升级时广播 `tier_up`，overlay 显示全屏段位升级动画。

### 7.4 SQLite 表结构

**建表时机：** server_v6.py 模块加载时立即执行 `_db.executescript()` + `_db.commit()`（4 张表同时创建），非延迟创建。

**users：** name, score, tier, total_coins, total_gifts, last_seen
  — `total_coins` / `total_gifts` 在 V6 中创建但**从不更新**，属死字段
**gift_log：** id, user, gift_name, slot_id, coins, timestamp
**round_history：** id, soup_id, difficulty, winner, duration, reveal_pct, timestamp
  — V6 中创建表但**从不写入记录**
**custom_soups：** id, title, surface, bottom, keywords, difficulty, source, created_at

SQLite 配置：`PRAGMA journal_mode=WAL`, `PRAGMA synchronous=NORMAL`，线程安全通过 `threading.Lock()` + `db_execute()`/`db_query()` 封装函数保证。

---

## 8. LLM 集成

### 8.1 三分类端点（llm_classify）

```
输入：汤底 + 关键词 + 弹幕
模型：deepseek-v4-flash（可配置 LLM_MODEL）
system: "判断弹幕与汤底的相关性。只回复：是、不是、是也不是"
temperature: 0.1, max_tokens: 10
输出：是 / 不是 / 是也不是
```

- classify 错误时默认返回"不是"（不打断流程）
- 结果记入 qa_history（保留最近 5 条用于 hint 上下文）

### 8.2 方向提示端点（llm_hint）

```
输入：qa_history[-5:] + 汤底 + 关键词
system: "生成方向引导提示。不超过20字。"
temperature: 0.7, max_tokens: 30
输出：方向提示文本
```

- 错误时返回默认提示："想想故事里谁最可疑？"
- 在 overlay 中下位置显示 hint-banner，60s 自动消失
- TTS 朗读提示文本

### 8.3 AI 出题端点（llm_generate_soups）

```
输入：difficulty, count(默认5)
生成要求：字数严格在难度区间内、故事合理有反转、关键词3-5个
返回: JSON 数组 [{surface, bottom, keywords, difficulty}]
temperature: 0.9, max_tokens: 2000
```

- 返回结果含有 answer_length（实际字数）
- 审核通过（ai-approve）才入库到 custom_soups 表
- 解析 JSON 时兼容 ` ```json...``` ` 包裹格式

---

## 9. 投屏端（overlay.html）9:16 竖屏

### 9.1 布局结构（自上而下）

| 区域 | 高度 | 内容 |
|------|------|------|
| 顶栏 | 5.5vh | logo + 标题 + 难度标签 + 状态标签 |
| 汤面区 | 11-15vh | ✦ 前缀 + 📖 装饰 + 2行截断 |
| 谜底揭示区 | 38-55vh | 🔮 标题 + 揭示统计 + 字格网格 |
| 进度条 | ~1.1vh | 渐变色 + 闪光动画 + 警告态(>33% 黄色渐变) + 危险态(>66% 红色脉冲) |
| 底部互动区 | 14vh | 弹幕面板(左侧 3/5) + 排行面板(右侧 2/5) |

### 9.2 安全区设计

- 视口 1080×1920（OBS 竖屏推流）
- 实际内容区域：中心 1080×1080，避开抖音底部互动栏和顶部状态栏
- 单位：vh/vw 响应式

### 9.3 动画系统

| 动画（@keyframes 名） | 触发条件 |
|------|---------|
| reveal-pop | 新字揭示（缩放 + 旋转入） |
| highlight-pulse | 高亮线索礼物（金色放大闪烁） |
| gift-fly-in | 普通礼物（中心飞入→弹跳→上浮消失，4s） |
| fullscreen-flash | 高价礼物 ≥1000抖币（全屏射线旋转 + 图标放大，2.5s），对应 CSS class `.gift-fullscreen` |
| tier-flash | 段位升级（全屏黑色蒙版 + 徽章旋转展示，3.5s） |
| bubble-in | 礼物气泡（右侧从右飞入→渐隐，3s） |
| bubble-pop | 答题气泡（左侧从左飞入→渐隐，3s） |
| hint-bounce | 提示横幅（中下弹性飞入→60s后渐隐向上消失） |
| settle-up | 通关结算（底部上滑弹窗，5s消失） |
| tts-in | TTS 字幕条（底部居中从下浮入） |
| shimmer | 进度条闪光 |
| danger-pulse | 进度 >66% 红色脉冲 |
| logo-pulse | logo 光晕呼吸 |

### 9.4 弹幕渲染

- 最大保留 40 条（CONFIG.maxDanmaku）
- 显示：用户昵称（金色）+ 弹幕内容 + 分类标签（✓/✗/△）
- 分类标签语义：`✓` = 是（命中，加分）、`✗` = 不是（未命中）、`△` = 是也不是（部分命中，加 half 分）、`?` = 未知分类（LLM 异常兜底）
- 弹幕从左侧飞入动画
- 新局开局时清空

> **注意**：overlay.html 的 CSS（约第 420-442 行）已定义 `.answer-bubbles` 容器和 `.answer-bubble` 气泡样式（含 `.yes`/`.no`/`.maybe` 三色变体），但 JS `updateQA()` 为存根函数，直接 return。答题气泡仅有样式层，未实现 DOM 创建逻辑。

### 9.5 段位排行榜

- 读取 stats.tier_dist → 按人数排序 → 显示前 5
- 排名 1-3 用 🥇🥈🥉，其余显示数字
- 无数据时显示"暂无排行"

### 9.6 TTS（Web Speech API）

```
SpeechSynthesisUtterance
lang: zh-CN, rate: 1.0, pitch: 1.0, volume: 0.8
触发时机：
  - game_start：朗读"汤面：xxx"（8s）
  - hint：朗读"提示：xxx"（6s）
  - game_end：朗读"谜底揭晓：xxx"（12s）（汤底文字仅 TTS 朗读，结算弹窗不渲染答案文字）
显示：底部 tts-bar 悬浮条，文本+🔊图标
```

### 9.7 粒子背景

- Canvas 2D 绘制 30 个光点
- 光点颜色 rgba(0,212,255,0.4)，缓慢移动
- 窗口 resize 自适应

---

## 10. 控制台端（admin.html）5 Tab

| Tab | 功能 |
|-----|------|
| 🎮 游戏控制 | 汤面预览 + 字格揭示 + 难度选择5档 + 开始/揭晓/重置 + 操作日志 |
| 🎁 礼物槽位 | 10槽网格（5效果+5难度）+ 点击弹窗从368礼物中搜索绑定 |
| 📚 题库管理 | 按难度筛选 + 列表 + 使用/删除 + 新增弹窗 + 批量导入JSON |
| 🤖 AI 出题 | AI生成5道 → 审核列表 → 入库/废弃 |
| 📈 数据 | 4指标卡（积分/弹幕/礼物/付费用户）+ 最近礼物列表 |

> **注意**：admin.js 的 `handleMessage()` 未处理 `slots_updated` 消息。服务端槽位变更后广播该消息，但 admin 面板无对应 case → 双 admin tab 不同步（需刷新页面手动同步）。

---

## 11. 仪表盘端（dashboard.html）

| 卡片 | 内容 |
|------|------|
| 在线观众 | 当前人数 + 5分钟增量 |
| 弹幕/分钟 | 速率 + 总数 |
| 礼物/分钟 | 速率 + 总收入抖币 |
| 付费率 | 百分比 + 付费用户数 |
| 弹幕趋势图 | Canvas 折线图（最近30分钟，蓝色渐变填充） |
| 礼物趋势图 | Canvas 折线图（最近30分钟，金色渐变填充） |
| 排行榜 TOP 10 | 排名 + 段位标签 + 积分 |
| 礼物 TOP 10 | 礼物名 + 次数 + 总抖币 |
| 段位分布 | 10段位每段人数 |

- Canvas 自绘折线图（无第三方图表库依赖）
- 数据每 10s 轮询排行榜，每 30s 轮询段位分布
- WebSocket 接收 state_sync/metrics_update 实时更新

---

## 12. 抖音弹幕桥接（dy_bridge.py）

### 12.1 架构

```
douyinLive.exe (port 1088, WebSocket 推流)
  → dy_bridge.py (websockets 客户端)
    → HTTP POST → server_v6.py /api/barrage/push
```

### 12.2 消息转换

| douyinLive 原始 type | 转换后 payload |
|---------------------|---------------|
| `danmaku` | `{type, user, content}` |
| `gift` | `{type, user, gift_name, diamond_count, count}` |
| `like` / `member` / `follow` | `{type, user, count}`（dy_bridge 已转发至 `/api/barrage/push`，但服务端该端点仅接受 `PushDanmakuReq`（需含 `content` 字段），like/member/follow 负载缺 `content` → Pydantic 校验失败，静默丢弃。礼物消息也存在同样问题——payload 缺 `content`，通过同一端点发送时也被静默拒绝。如需处理需新增专用端点或扩展 push 路由） |

### 12.3 配置

```
LIVE_WS_URL: douyinLive 的 WS 地址（默认 ws://localhost:1088）
SERVER_PUSH_URL: server 的推流入口（默认 /api/barrage/push）
自动重连间隔: 3s
```

> **注意**：`_forward()` 遇到 `json.JSONDecodeError`（非 JSON 消息）时 `pass` 静默忽略，不产生日志。douyinLive 发送的无效 JSON 不会被察觉。

---

## 13. 配置项（.env 文件）

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `LLM_API_KEY` | (空) | DeepSeek API 密钥 |
| `LLM_BASE_URL` | `https://api.deepseek.com` | LLM API 地址 |
| `LLM_MODEL` | `deepseek-v4-flash` | 模型名 |
| `SERVER_PORT` | `3010` | 服务端口 |
| `LIVE_WS_URL` | `ws://localhost:1088` | douyinLive WS 地址 |
| `BRIDGE_ENABLED` | `true` | 是否启动桥接 |
| `V6_MODE` | `self_hosted` | 运行模式（self_hosted / licensed） |
| `ANTI_STALL_INTERVAL` | `180` | 防卡死基础间隔(秒) |
| `ANTI_STALL_DANMAKU` | `50` | 防卡死弹幕阈值(条) |
| `ANTI_STALL_DECAY` | `0.8` | 防卡死衰减系数 |
| `LICENSE_KEY` | (空) | 授权密钥（预留） |
| `LICENSE_MODE` | `trial` | 授权模式（预留） |

---

## 14. 部署方案

### 14.1 单机模式（推荐）

```
同一台电脑：
  1. 启动 douyinLive.exe（后台）
  2. python server_v6.py（port 3010）
  3. Chrome 打开 http://localhost:3010/admin（控制台）
  4. OBS 添加浏览器源 http://localhost:3010/overlay
     - 宽度 1080，高度 1920（竖屏）
     - 关闭"控制音频"（TTS 走系统音频）
  5. （可选）Chrome 新标签页打开 /dashboard 看数据
```

### 14.2 双机模式（中大型主播）

```
直播电脑（A机，推流专用）：
  OBS 浏览器源 → http://192.168.x.x:3010/overlay

主播电脑（B机，操作专用）：
  douyinLive.exe + python server_v6.py
  Chrome → /admin + /dashboard
```

### 14.3 启动脚本（start_all.bat）

```batch
@echo off
start "" /B python backend/server_v6.py
timeout /t 3 >nul
start http://localhost:3010/admin
```

---

## 15. 题库

### 15.1 内置题库（data_soups_v6.py）

- 15 题，覆盖 3 个难度（easy×4, medium×6, hard×5）。hell 和 void 无内置题
- 题目按汤底字数自动归类（classify_by_length），启动时校验 difficulty 字段是否与字数匹配（标注 _auto_difficulty / _difficulty_mismatch）
- **开局选题 fallback**：当按 `difficulty` 过滤无候选时（如选 hell/void），`start_game()` 从全部 SOUPS 随机选一道。但若 SOUPS 也为空（无内置题且 custom_soups 为空），`random.choice([])` 抛 IndexError → 游戏崩溃。V7 应在 fallback 前增加空列表检查。

### 15.2 自定义题库（SQLite custom_soups 表）

- 来源：manual（手动添加）/ import（批量导入）/ ai（AI生成审核后入库）
- 支持增删改查 API
- 开局时优先选择指定 soup_id，否则按 difficulty 过滤随机选

### 15.3 AI 出题流程

1. 管理员在 admin 点击 "AI 生成" → POST /api/admin/ai-generate
2. LLM 返回 5 道候选 → 展示在 AI 审核 tab
3. 管理员逐条 "入库"（POST /api/admin/ai-approve）或 "废弃"
4. 入库后存入 custom_soups 表，后续开局可用

---

## 16. 数据统计（滑动窗口）

- 弹幕和礼物速率按分钟聚合，保留最近 30 个点（30 分钟滑动窗口）
- 超出窗口的数据被丢弃
- metrics API 返回：viewers, danmaku_rate, gift_rate, pay_rate, leaderboard, gift_list, tier_dist
- 趋势图从 danmaku_series / gift_series 绘制

> **⚠ 滑动窗口 Bug**：`_record_series` 使用 `_series_start`（仅初始化一次，永不 reset）。30 分钟后 `elapsed >= 30`，while 循环不再追加新桶，所有新数据累加到 `series[-1]`。趋势图失真。应在 `GameRoom.reset()` 中重置 `_series_start`。

> **⚠ stats.leaderboard/stats.viewers 从未更新**：GameRoom.stats 初始化了 `leaderboard: []` 和 `viewers: 0`，但没有任何代码更新这两个字段。metrics API 始终返回空值和 0。需补充更新逻辑。

> **⚠ _series_start 不在 reset() 中重置**：`GameRoom.reset()` 不清除 `_series_start`。速率计算实际是"服务启动以来的终生均值"而非"当前局"。

---

## 17. 授权模型（预留）

- 当前代码中 config 加载了 LICENSE_KEY / LICENSE_MODE，但未实现校验逻辑
- V6_MODE = self_hosted（默认）免授权
- V6_MODE = licensed 时需要校验授权（待实现）
- 四个授权级别：trial / personal / team / lifetime（待实现）

---

## 18. 已知限制与遗留问题

1. **段位升级推送**：tier_up 广播中 `msg.to_tier` 和 `msg.from_tier` 传的是 dict，但 overlay.js 的 showTierUp 用 `msg.to_tier.name` 访问，实际 `msg.to_tier` 是 {name, icon} 对象 → 需确认序列化正确
2. **新用户初始分逻辑反直觉**：`add_score` 中新用户 `new_score = max(100, delta)`。首次答对 15 分被拉到 100 → 直接到青铜段位（门槛 100）。应改为 `max(0, delta)`。
3. **段位榜**：dashboard 的 tier_dist 写死了 10 个段位名称硬编码（黑铁→超级王者），应当从 TIERS 动态读取
4. **overlay 排行数据源**：overlay 目前读取 stats.tier_dist 渲染排行榜，但 tier_dist 只在 metrics API 中更新，state_sync 时可能为空 → 需确保 state_sync 携带完整 stats
5. **overlay showTierUp**：`msg.to_tier` 实际上是段位 {id, name, color, min_score, icon} 对象，但模板里用 `msg.to_tier.name` 可行；只是字段引用路径需验证
6. **弹幕推流双写**：dy_bridge 推流到 `/api/barrage/push`，直接调用 handle_danmaku。如果有 WS 客户端也发送相同弹幕 → 重复处理。需要在桥接端做去重（如 msg_id）
7. **aliases 未使用**：GIFT_ALIASES 定义了但 resolve_gift_to_slot 未真正使用它们（只查了 slot_manager.resolve_gift）
8. **anti_stall 衰减无下限**：180s 基础间隔 × decay^triggers（每次减少 20%），10 次后 180×0.8^10 ≈ 19s。无最小间隔保护 → 越触发越快，应设下限（如 min=30s）。
9. **权限**：无登录/认证，任何人连接 WS 可控制游戏。适合直播环境（内网），公网部署需加认证

10. **`/api/admin/reset-session` 路由不存在**：dashboard.html 的"重置会话"按钮调用此路由，server_v6.py 未实现 → 点击得到 404。

11. **`_difficulty_mismatch` 静默计算不输出**：data_soups_v6.py 启动时比对难度标签与汤底字数，设 `_auto_difficulty` 和 `_difficulty_mismatch`，但不打印日志也不暴露 API。

12. **普通礼物同时触发两个动画**：`showGiftBubble()` 同时创建 `.gift-bubble`（右下气泡）+ `.gift-fly`（中心飞入）两个 DOM 元素。非高价礼物触发双重动画。

13. **代码注释与实际矛盾**：server_v6.py handle_danmaku 中注释写"注意：不广播 reveal_update，只发 classification"，紧接着的下一行是 `await manager.broadcast({"type": "reveal_update", ...})`。注释是旧版残留。

14. **admin.js WS 消息处理不完整**：admin 端 `handleMessage()` 仅处理 state_sync/game_start/reveal_update/game_end/classification/gift_effect 共 6 种消息，忽略 tier_up/score_update/hint/difficulty_scheduled/slots_updated/metrics_update。主播端看不到段位升级、积分变化、提示横幅和难度切换预告。

15. **答题气泡 JS 存根**：overlay.js `updateQA(msg.answerType)` 收到分类消息后直接 `if (answerType === 'hint') return;` 空返回。答题气泡的 CSS（`.answer-bubble.yes/.no/.maybe`）已完整定义，但 DOM 创建逻辑未实现，气泡功能不存在。

16. **DIFFICULTY_MULTIPLIER 双定义**：tiers.py 和 data_soups_v6.py 各自定义了 `DIFFICULTY_MULTIPLIER` 字典，值一致（×1.0/1.5/2.0/3.0/5.0）。但 server_v6.py `handle_danmaku` 实际使用的是 `DIFFICULTY_CONFIG[difficulty].multiplier`（来自 data_soups_v6.py），tiers.py 的版本未被引用。两份定义无数据差异，但存在混淆风险。

17. **admin 数据 Tab 指标永为 0**：admin.js 的 WS `handleMessage()` 仅处理 6 种消息（见 §18.14），切换到"数据"Tab 时也不触发 API 轮询。4 个指标卡（本局积分总数/本局弹幕数/本局礼物数/付费用户数）初始化后永不更新，`recentGifts` 列表也从不填充。

18. **`danmu` WS 消息类型存在但无发送方**：overlay.js `handleMessage()` 中有 `case 'danmu'` 处理外部推流弹幕（跳过 LLM 分类直接显示），但 server_v6.py 从不广播此类型。属前端预留/存根代码。

19. **dy_bridge 缺少运维接口**：`DouyinBridge.get_stats()` 返回 `{connected, forwarded, errors}` 三个计数器，但未暴露为 HTTP API，无外部监控手段。

20. **Dashboard 指标卡/图表无数据源**：dashboard.js 的 `handleMessage()` 从 `msg.metrics` 读取数据，但 WS `state_sync` 的 metrics 在 `msg.room.stats` 而非 `msg.metrics`；`metrics_update` 消息服务端从不广播。dashboard 也不轮询 `/api/admin/metrics`。结果：4 指标卡永为 0、2 个 Canvas 趋势图永为"等待数据..."。段位排行榜（轮询 `/api/leaderboard`）和段位分布（轮询 `/api/admin/tier-dist`）功能正常。

21. **WS `json.loads(raw)` 无保护**：`websocket_endpoint()` line 753 的 `json.loads(raw)` 在 try/except 块之外（仅在 except WebSocketDisconnect 中）。客户端发送非 JSON 消息时抛出 `JSONDecodeError` → 连接断开但 `manager.disconnect(cid)` 不执行 → 连接泄漏。同时 `start_game()` 的 `random.choice()` 在 SOUPS 为空时无保护，抛 IndexError。

---

## 附录：V6 → V7 变化摘要

| 项目 | V6（当前代码） | V7 改动 |
|------|---------------|---------|
| 三端分离 | admin/dashboard/overlay 三个独立 HTML | 维持，微调 JS 错误（showTierUp 字段） |
| 礼物系统 | 10 槽位（5+5），368 礼物库 | 维持，补全 aliases 实际使用 |
| LLM | classify + hint + generate，三端 | 维持 |
| 防卡死 | 180s 基础，0.8 衰减 | 增加最小间隔下限（建议 30s） |
| 段位 | 10 级硬编码 | 动态从 TIERS 读取 |
| 授权 | config 加载但不校验 | 维持预留状态 |
| overlay 排行 | 从 tier_dist 渲染 | 改为从 leaderboard API 或 state_sync.stats 取 |
| 弹幕推流 | 桥接 + WS 双写风险 | 增加 msg_id 去重（可选） |
